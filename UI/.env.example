# Frontend API Configuration (Vite)
# For Vite, use VITE_ prefix instead of REACT_APP_
VITE_API_URL=http://localhost:8080

# For development with Vite proxy, you can use relative URLs
# The proxy in vite.config.js will handle the routing
